/* cmd_table.inc — single source of truth for shell builtins */

#define CMD(n, d, f) { n, d, f },

static const shell_cmd_entry_t shell_cmd_table[] = {
    CMD("alias",       "Define command alias (alias name='value')",           cmd_alias)
    CMD("arp",         "Show ARP cache",                                        sh_arp)
    CMD("awk",         "Simple AWK-like field processor",                       cmd_awk)
    CMD("base64",      "Encode file as base64",                                 cmd_base64)
    CMD("basename",    "Extract filename from path",                            cmd_basename)
    CMD("beep",        "PC speaker beep (beep [freq] [ms])",                    cmd_beep)
    CMD("bg",          "Resume stopped job in background",                      cmd_bg)
    CMD("calc",        "Calculator (calc <expr>)",                              cmd_calc)
    CMD("capprof",     "Set syscall profile (capprof <pid> <none|default|trusted>)", cmd_capprof)
    CMD("cat",         "Show file contents",                                    cmd_cat)
    CMD("cc",          "Compile C source to ELF64 (cc [-c] <file> [out])",      cmd_cc)
    CMD("ccbuilder",   "Run build manifest (ccbuilder <manifest>)",             cmd_ccbuilder)
    CMD("ld",          "Link .o files into ELF64 (ld [-o out] f1.o ...)",       cmd_ld)
    CMD("cd",          "Change directory",                                      cmd_cd)
    CMD("chmod",       "Change file permissions (chmod <mode> <path>)",         cmd_chmod)
    CMD("chown",       "Change file owner (chown <uid>[:gid] <path>)",          cmd_chown)
    CMD("clear",       "Clear screen",                                          sh_clear)
    CMD("cmos",        "Show CMOS hardware configuration",                      sh_cmos)
    CMD("color",       "Set VGA color (color <fg> [bg])",                       cmd_color)
    CMD("comm",        "Compare two sorted files (comm [-123] <f1> <f2>)",      cmd_comm)
    CMD("cp",          "Copy file (cp <src> <dst>)",                            cmd_cp)
    CMD("cpuinfo",     "Show CPU info",                                         sh_cpuinfo)
    CMD("curl",        "HTTP GET (curl [-F] <url>)",                            cmd_curl)
    CMD("cut",         "Extract field from delimited file",                     cmd_cut)
    CMD("date",        "Show RTC date/time",                                    sh_date)
    CMD("df",          "Show disk usage",                                       sh_df)
    CMD("diff",        "Compare two files",                                     cmd_diff)
    CMD("dirname",     "Extract directory from path",                           cmd_dirname)
    CMD("dmesg",       "Show boot log",                                         sh_dmesg)
    CMD("dns",         "Resolve hostname (dns <host>)",                         cmd_dns)
    CMD("doom",        "Play Doom-like raycast shooter",                        cmd_doom)
    CMD("du",          "Disk usage by directory",                               cmd_du)
    CMD("echo",        "Print arguments",                                       cmd_echo)
    CMD("edit",        "Text editor (edit <file>)",                             editor_open)
    CMD("env",         "Show environment variables",                            sh_env)
    CMD("exec",        "Execute ELF binary (exec <path>)",                      cmd_exec)
    CMD("expand",      "Convert tabs to spaces (expand [-t N] <file>)",           cmd_expand)
    CMD("expr",        "Evaluate integer expression",                           cmd_expr)
    CMD("false",       "Return failure exit status",                            cmd_false)
    CMD("fat",         "FAT32 filesystem (mount/ls/cat/write/sync/mkdir/unlink)", cmd_fat)
    CMD("fbinfo",      "Show framebuffer info",                                 sh_fbinfo)
    CMD("fg",          "Bring job to foreground (fg <pid|%%job>)",                cmd_fg)
    CMD("file",        "Show file type",                                        cmd_file)
    CMD("find",        "Find files matching glob",                              cmd_find)
    CMD("fold",        "Wrap lines to width (fold [-w N] <file>)",                cmd_fold)
    CMD("format",      "Format SMFS filesystem",                                sh_format)
    CMD("free",        "Show memory usage",                                     sh_free)
    CMD("grep",        "Search text in file",                                   cmd_grep)
    CMD("gui",         "Launch GUI desktop (experimental)",                     sh_gui)
    CMD("head",        "Show first N lines (head <file> [n])",                    cmd_head)
    CMD("help",        "Show this help",                                        sh_help)
    CMD("hexdump",     "Dump memory (hexdump <addr> [len])",                    cmd_hexdump)
    CMD("history",     "Show command history",                                  sh_history)
    CMD("hostname",    "Show hostname",                                         sh_hostname)
    CMD("hwinfo",      "Comprehensive hardware summary",                        sh_hwinfo)
    CMD("id",          "Print user identity",                                   cmd_id)
    CMD("ifconfig",    "Show network configuration",                            sh_ifconfig)
    CMD("jobs",        "List background jobs",                                  sh_jobs)
    CMD("kill",        "Kill process (kill <pid> [signal])",                    cmd_kill)
    CMD("ln",          "Link (copy) a file",                                    cmd_ln)
    CMD("login",       "Log in as a user",                                      cmd_login)
    CMD("logout",      "Log out current user",                                  sh_logout)
    CMD("ls",          "List files [dir]",                                      cmd_ls)
    CMD("lsblk",       "List block devices",                                    sh_lsblk)
    CMD("lspci",       "List PCI devices",                                      sh_lspci)
    CMD("lsusb",       "List USB devices",                                      sh_lsusb)
    CMD("md5sum",      "MD5 hash of a file",                                    cmd_md5sum)
    CMD("meminfo",     "Show memory info",                                      sh_meminfo)
    CMD("mkdir",       "Create directory",                                      cmd_mkdir)
    CMD("more",        "Page through a file",                                   cmd_more)
    CMD("mouse",       "Show mouse position and buttons",                       sh_mouse)
    CMD("mv",          "Move/rename file",                                      cmd_mv)
    CMD("nc",          "Netcat (nc <host> <port> [data])",                      cmd_nc)
    CMD("netstat",     "Show network connections",                              cmd_netstat)
    CMD("nice",        "Run command at priority (nice <pri> <cmd>)",            cmd_nice)
    CMD("nl",          "Number lines",                                          cmd_nl)
    CMD("nslookup",    "DNS lookup (nslookup <host>)",                          cmd_nslookup)
    CMD("od",          "Octal dump",                                            cmd_od)
    CMD("passwd",      "Change password",                                       cmd_passwd)
    CMD("paste",       "Merge file lines",                                      cmd_paste)
    CMD("ping",        "Ping host (ping [ip])",                                 cmd_ping)
    CMD("play",        "Play note sequence or PCM (play ...)",                  cmd_play)
    CMD("printf",      "Formatted output (printf <fmt> [args...])",             cmd_printf)
    CMD("ps",          "List processes",                                        sh_ps)
    CMD("pwd",         "Print working directory",                               sh_pwd)
    CMD("rawsend",     "Send raw Ethernet frame",                               cmd_rawsend)
    CMD("readlink",    "Read symbolic link target",                             cmd_readlink)
    CMD("reboot",      "Reboot system",                                         sh_reboot)
    CMD("renice",      "Change process priority",                               cmd_renice)
    CMD("rev",         "Reverse lines",                                         cmd_rev)
    CMD("rm",          "Remove file or empty directory",                        cmd_rm)
    CMD("route",       "Show routing table",                                    sh_route)
    CMD("run",         "Execute script file (run <path>)",                      cmd_run)
    CMD("sed",         "Stream editor (sed <expr> <file>)",                     cmd_sed)
    CMD("seq",         "Print number sequence",                                 cmd_seq)
    CMD("serial",      "COM1 serial port (status/write)",                       cmd_serial)
    CMD("service",     "Manage services (service start|stop|status)",           cmd_service)
    CMD("sha256sum",   "SHA-256 hash of a file",                                cmd_sha256sum)
    CMD("shutdown",    "Shutdown system (ACPI)",                                sh_shutdown)
    CMD("sleep",       "Sleep N seconds",                                       cmd_sleep)
    CMD("sort",        "Sort file lines",                                       cmd_sort)
    CMD("split",       "Split file into chunks",                                cmd_split)
    CMD("stat",        "Show file info",                                        cmd_stat_file)
    CMD("strings",     "Print printable strings from file",                     cmd_strings)
    CMD("tac",         "Print file lines in reverse",                           cmd_tac)
    CMD("tail",        "Show last N lines",                                     cmd_tail)
    CMD("tar",         "Simple tar archive (tar -c|-x ...)",                    cmd_tar)
    CMD("tee",         "Write stdin to file and stdout",                        cmd_tee)
    CMD("test",        "Evaluate condition ([ ] alias)",                        cmd_test)
    CMD("time",        "Time a command",                                        cmd_time)
    CMD("tmux",        "Terminal multiplexer",                                  cmd_tmux)
    CMD("top",         "Live process monitor",                                  sh_top)
    CMD("touch",       "Create empty file",                                     cmd_touch)
    CMD("trap",        "Set signal trap handler",                               cmd_trap)
    CMD("tr",          "Translate characters",                                  cmd_tr)
    CMD("true",        "Return success exit status",                            cmd_true)
    CMD("udpsend",     "Send UDP packet",                                       cmd_udpsend)
    CMD("uname",       "Show system info",                                      sh_uname)
    CMD("uniq",        "Remove duplicate adjacent lines",                       cmd_uniq)
    CMD("unalias",     "Remove alias (unalias <name>)",                         cmd_unalias)
    CMD("uptime",      "Show uptime",                                           sh_uptime)
    CMD("useradd",     "Add a user",                                            cmd_useradd)
    CMD("userdel",     "Delete a user",                                         cmd_userdel)
    CMD("users",       "List all users",                                        sh_users)
    CMD("wait",        "Wait for process (wait <pid>)",                         cmd_wait)
    CMD("watch",       "Run command repeatedly",                                cmd_watch)
    CMD("wc",          "Count lines/words/bytes",                               cmd_wc)
    CMD("wget",        "Download file (wget <url> [file])",                     cmd_wget)
    CMD("which",       "Locate a shell built-in",                               cmd_which)
    CMD("whoami",      "Show current user/process",                             sh_whoami)
    CMD("write",       "Write text to file",                                    cmd_write)
    CMD("xargs",       "Build args from pipe input",                            cmd_xargs)
    CMD("xxd",         "Hex dump file",                                         cmd_xxd)
    CMD("yes",         "Repeatedly output a string",                            cmd_yes)
    CMD("dosbox",      "Run DOS program under emulation",                        cmd_dosbox)
    CMD("shuf",        "Shuffle lines of a file randomly",                       cmd_shuf)
    CMD("logname",     "Print user's login name",                               cmd_logname)
    CMD("who",         "Show who is logged in",                                cmd_who)
    CMD("banner",      "Print large ASCII banner (banner <text>)",                cmd_banner)
    CMD("tsort",       "Topological sort (tsort <file>)",                        cmd_tsort)
    CMD("join",        "Join lines of two files (join [-j N] <f1> <f2>)",        cmd_join)
    CMD("sum",         "Checksum and count blocks (sum <file>)",                 cmd_sum)
    CMD("unexpand",    "Convert spaces to tabs (unexpand [-t N] <file>)",         cmd_unexpand)
    { 0, 0, 0 }
};

#undef CMD
