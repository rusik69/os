/* cmd_table.inc — single source of truth for shell builtins */

#define CMD(n, d, f) { n, d, f },

static const shell_cmd_entry_t shell_cmd_table[] = {
    CMD("alias",       "Define command alias (alias name='value')",           cmd_alias)
    CMD("arp",         "Show ARP cache",                                        sh_arp)
    CMD("awk",         "Simple AWK-like field processor",                       cmd_awk)
    CMD("base64",       "Base64 encode/decode",                                 cmd_base64)
    CMD("basename",     "Extract filename from path",                            cmd_basename)
    CMD("basenc",       "Various encodings (basenc --base64 ...)",               cmd_basenc)
    CMD("bc",           "Calculator (bc <expr> with variables a-z)",             sh_bc)
    CMD("beep",        "PC speaker beep (beep [freq] [ms])",                    cmd_beep)
    CMD("bg",          "Resume stopped job in background",                      cmd_bg)
    CMD("bt",          "Print kernel backtrace",                                sh_bt)
    CMD("calc",        "Calculator (calc <expr>)",                              cmd_calc)
    CMD("capprof",     "Set syscall profile (capprof <pid> <none|default|trusted>)", cmd_capprof)
    CMD("cat",         "Show file contents",                                    cmd_cat)
    CMD("cc",          "Compile C source to ELF64 (cc [-c] <file> [out])",      cmd_cc)
    CMD("ccbuilder",   "Run build manifest (ccbuilder <manifest>)",             cmd_ccbuilder)
    CMD("ld",          "Link .o files into ELF64 (ld [-o out] f1.o ...)",       cmd_ld)
    CMD("cd",          "Change directory",                                      cmd_cd)
    CMD("chmod",       "Change file permissions (chmod <mode> <path>)",         cmd_chmod)
    CMD("chown",       "Change file owner (chown <uid>[:gid] <path>)",          cmd_chown)
    CMD("clear",       "Clear screen",                                          sh_clear)
    CMD("cmos",        "Show CMOS hardware configuration",                      sh_cmos)
    CMD("color",       "Set VGA color (color <fg> [bg])",                       cmd_color)
    CMD("comm",        "Compare two sorted files (comm [-123] <f1> <f2>)",      cmd_comm_wrapper)
    CMD("cp",          "Copy file (cp <src> <dst>)",                            cmd_cp)
    CMD("cpuinfo",     "Show CPU info",                                         sh_cpuinfo)
    CMD("curl",        "HTTP GET (curl [-F] <url>)",                            cmd_curl)
    CMD("cut",         "Extract field from delimited file",                     cmd_cut)
    CMD("date",        "Show RTC date/time",                                    sh_date)
    CMD("df",          "Show disk usage",                                       sh_df)
    CMD("diff",        "Compare two files",                                     cmd_diff)
    CMD("ncdu",        "Disk usage analyzer (ncdu [path])",                      cmd_ncdu)
    CMD("fm",          "File manager (two-pane directory browser)",               cmd_fm)
    CMD("mc",          "File manager (alias for fm)",                            cmd_mc)
    CMD("dirname",     "Extract directory from path",                           cmd_dirname)
    CMD("dmesg",       "Show boot log",                                         sh_dmesg)
    CMD("dns",         "Resolve hostname (dns <host>)",                         cmd_dns)
    CMD("doom",        "Play Doom-like raycast shooter",                        cmd_doom)
    CMD("du",          "Disk usage by directory",                               cmd_du)
    CMD("echo",        "Print arguments",                                       cmd_echo)
    CMD("edit",        "Text editor (edit <file>)",                             editor_open)
    CMD("env",         "Show environment variables",                            sh_env)
    CMD("exec",        "Execute ELF binary (exec <path>)",                      cmd_exec)
    CMD("expand",      "Convert tabs to spaces (expand [-t N] <file>)",           cmd_expand_wrapper)
    CMD("expr",        "Evaluate integer expression",                           cmd_expr)
    CMD("false",       "Return failure exit status",                            sh_false)
    CMD("fat",         "FAT32 filesystem (mount/ls/cat/write/sync/mkdir/unlink)", cmd_fat)
    CMD("fbinfo",      "Show framebuffer info",                                 sh_fbinfo)
    CMD("fg",          "Bring job to foreground (fg <pid|%%job>)",                cmd_fg)
    CMD("file",        "Show file type",                                        cmd_file)
    CMD("find",        "Find files matching glob",                              cmd_find)
    CMD("fold",        "Wrap lines to width (fold [-w N] <file>)",                cmd_fold_wrapper)
    CMD("format",      "Format SMFS filesystem",                                sh_format)
    CMD("free",        "Show memory usage",                                     sh_free)
    CMD("getty",       "Serial console login daemon (getty [baud] [line] [term])", cmd_getty)
    CMD("grep",        "Search text in file",                                   cmd_grep)
    CMD("gui",         "Launch GUI desktop (experimental)",                     sh_gui)
    CMD("head",        "Show first N lines (head <file> [n])",                    cmd_head)
    CMD("help",        "Show this help",                                        sh_help)
    CMD("hexdump",     "Dump memory (hexdump <addr> [len])",                    cmd_hexdump)
    CMD("history",     "Show command history",                                  sh_history)
    CMD("hostname",    "Show hostname",                                         sh_hostname)
    CMD("hwinfo",      "Comprehensive hardware summary",                        sh_hwinfo)
    CMD("id",          "Print user identity",                                   cmd_id)
    CMD("ifconfig",    "Show network configuration",                            sh_ifconfig)
    CMD("jobs",        "List background jobs",                                  sh_jobs)
    CMD("kill",        "Kill process (kill <pid> [signal])",                    cmd_kill)
    CMD("ln",          "Link (copy) a file",                                    cmd_ln)
    CMD("login",       "Log in as a user",                                      cmd_login)
    CMD("logout",      "Log out current user",                                  sh_logout)
    CMD("ls",          "List files [dir]",                                      cmd_ls)
    CMD("lsblk",       "List block devices",                                    sh_lsblk)
    CMD("lspci",       "List PCI devices",                                      sh_lspci)
    CMD("lsusb",       "List USB devices",                                      sh_lsusb)
    CMD("md5sum",      "MD5 hash of a file",                                    cmd_md5sum)
    CMD("mdev",        "Device node manager (mdev [-s])",                        sh_mdev)
    CMD("meminfo",     "Show memory info",                                      sh_meminfo)
    CMD("mkdir",       "Create directory",                                      cmd_mkdir)
    CMD("more",        "Page through a file",                                   cmd_more)
    CMD("mouse",       "Show mouse position and buttons",                       sh_mouse)
    CMD("mv",          "Move/rename file",                                      cmd_mv)
    CMD("nc",          "Netcat (nc <host> <port> [data])",                      cmd_nc)
    CMD("neofetch",    "Display system information with logo",                  cmd_neofetch_wrapper)
    CMD("netstat",     "Show network connections",                              cmd_netstat)
    CMD("nice",        "Run command at priority (nice <pri> <cmd>)",            cmd_nice)
    CMD("nl",          "Number lines",                                          cmd_nl)
    CMD("nvme",        "NVMe device management (nvme list|sanitize)",          cmd_nvme)
    CMD("nslookup",    "DNS lookup (nslookup <host>)",                          cmd_nslookup)
    CMD("od",          "Octal dump",                                            cmd_od)
    CMD("passwd",      "Change password",                                       cmd_passwd)
    CMD("paste",       "Merge file lines",                                      cmd_paste)
    CMD("ping",        "Ping host (ping [ip])",                                 cmd_ping)
    CMD("play",        "Play note sequence or PCM (play ...)",                  cmd_play)
    CMD("printf",      "Formatted output (printf <fmt> [args...])",             cmd_printf)
    CMD("ps",          "List processes",                                        sh_ps)
    CMD("pwd",         "Print working directory",                               sh_pwd)
    CMD("rawsend",     "Send raw Ethernet frame",                               cmd_rawsend)
    CMD("readlink",    "Read symbolic link target",                             cmd_readlink)
    CMD("reboot",      "Reboot system",                                         sh_reboot)
    CMD("renice",      "Change process priority",                               cmd_renice)
    CMD("rev",         "Reverse lines",                                         cmd_rev)
    CMD("rm",          "Remove file or empty directory",                        cmd_rm)
    CMD("route",       "Show routing table",                                    sh_route)
    CMD("run",         "Execute script file (run <path>)",                      cmd_run)
    CMD("sed",         "Stream editor (sed <expr> <file>)",                     cmd_sed)
    CMD("seq",         "Print number sequence",                                 cmd_seq_wrapper)
    CMD("serial",      "COM1 serial port (status/write)",                       cmd_serial)
    CMD("service",     "Manage services (service start|stop|status)",           cmd_service)
    CMD("sha256sum",   "SHA-256 hash of a file",                                cmd_sha256sum)
    CMD("shutdown",    "Shutdown system (ACPI)",                                sh_shutdown)
    CMD("sleep",       "Sleep N seconds",                                       cmd_sleep)
    CMD("sort",        "Sort file lines",                                       cmd_sort)
    CMD("split",       "Split file into chunks",                                cmd_split)
    CMD("stat",        "Show file info",                                        cmd_stat_file)
    CMD("su",          "Substitute user identity (su [-] [user])",               sh_su)
    CMD("sudo",        "Execute a command as superuser (sudo [-u user] <cmd>)",   cmd_sudo)
    CMD("strings",     "Print printable strings from file",                     cmd_strings)
    CMD("sum",         "Checksum and count blocks (sum <file>)",                 cmd_sum)
    CMD("sync",        "Flush filesystem buffers",                               sh_sync)
    CMD("tac",         "Print file lines in reverse",                           cmd_tac)
    CMD("tail",        "Show last N lines",                                     cmd_tail)
    CMD("tar",         "Simple tar archive (tar -c|-x ...)",                    cmd_tar)
    CMD("tee",         "Write stdin to file and stdout",                        cmd_tee_wrapper)
    CMD("test",        "Evaluate condition ([ ] alias)",                        cmd_test)
    CMD("time",        "Time a command",                                        cmd_time)
    CMD("tmux",        "Terminal multiplexer",                                  cmd_tmux)
    CMD("top",         "Live process monitor",                                  sh_top)
    CMD("touch",       "Create empty file",                                     cmd_touch)
    CMD("trap",        "Set signal trap handler",                               cmd_trap)
    CMD("tr",          "Translate characters",                                  cmd_tr)
    CMD("true",        "Return success exit status",                            sh_true)
    CMD("udpsend",     "Send UDP packet",                                       cmd_udpsend)
    CMD("uname",       "Show system info",                                      sh_uname)
    CMD("uniq",        "Remove duplicate adjacent lines",                       cmd_uniq)
    CMD("unalias",     "Remove alias (unalias <name>)",                         cmd_unalias)
    CMD("uptime",      "Show uptime",                                           sh_uptime)
    CMD("useradd",     "Add a user",                                            cmd_useradd)
    CMD("userdel",     "Delete a user",                                         cmd_userdel)
    CMD("users",       "List all users",                                        sh_users)
    CMD("wait",        "Wait for process (wait <pid>)",                         cmd_wait)
    CMD("watch",       "Run command repeatedly",                                cmd_watch)
    CMD("wc",          "Count lines/words/bytes",                               cmd_wc)
    CMD("wget",        "Download file (wget <url> [file])",                     cmd_wget)
    CMD("which",       "Locate a shell built-in",                               cmd_which)
    CMD("whoami",      "Show current user/process",                             sh_whoami)
    CMD("write",       "Write text to file",                                    cmd_write)
    CMD("xargs",       "Build args from pipe input",                            cmd_xargs)
    CMD("xxd",         "Hex dump file",                                         cmd_xxd)
    CMD("yes",         "Repeatedly output a string",                            cmd_yes_wrapper)
    CMD("dosbox",      "Run DOS program under emulation",                        cmd_dosbox)
    CMD("shuf",        "Shuffle lines of a file randomly",                       cmd_shuf)
    CMD("logname",     "Print user's login name",                               sh_logname)
    CMD("who",         "Show who is logged in",                                cmd_who)
    CMD("banner",      "Print large ASCII banner (banner <text>)",                cmd_banner)
    CMD("tsort",       "Topological sort (tsort <file>)",                        cmd_tsort_wrapper)
    CMD("join",        "Join lines of two files (join [-j N] <f1> <f2>)",        cmd_join_wrapper)
    CMD("unexpand",    "Convert spaces to tabs (unexpand [-t N] <file>)",         cmd_unexpand_wrapper)
    /* Phase 8 — new shell commands */
    CMD("arch",        "Print machine architecture",                                 cmd_arch)
    CMD("chgrp",       "Change file group ownership",                                cmd_chgrp)
    CMD("chrt",        "Get/set real-time scheduling attributes",                    cmd_chrt)
    CMD("cmp",         "Compare two files byte-by-byte",                             cmd_cmp)
    CMD("factor",      "Factor integers into prime factors",                         cmd_factor)
    CMD("fmt",         "Simple text formatter (fold long lines)",                    cmd_fmt_wrapper)
    CMD("groups",      "Print group memberships for current user",                   sh_groups)
    CMD("hostid",      "Print the host/machine ID",                                  sh_hostid)
    CMD("link",        "Create hard link",                                           cmd_link)
    CMD("mknod",       "Create device node",                                         cmd_mknod)
    CMD("mktemp",      "Create temporary file and print its name",                   cmd_mktemp)
    CMD("nohup",       "Run command immune to hangups",                              cmd_nohup)
    CMD("pathchk",     "Check pathname validity/readability",                        cmd_pathchk)
    CMD("printenv",    "Print all shell environment variables",                      cmd_printenv)
    CMD("realpath",    "Print resolved absolute path",                               cmd_realpath)
    CMD("rmdir",       "Remove empty directory",                                     cmd_rmdir)
    CMD("shred",       "Overwrite file with random data before unlinking",           cmd_shred)
    CMD("size",        "Print size of sections in ELF file",                         cmd_size)
    CMD("ssh",         "SSH client (ssh <user>@<host>[:port])",                       cmd_ssh)
    CMD("sshd",        "Manage SSH server (sshd start|stop|status)",                  cmd_sshd)
    CMD("taskset",     "Get/set CPU affinity for processes",                         cmd_taskset)
    CMD("truncate",    "Shrink or extend file to specified size",                    cmd_truncate)
    CMD("tty",         "Print terminal device name",                                 sh_tty)
    CMD("unlink",      "Unlink/delete file by path",                                 cmd_unlink)
    /* Phase 9 -- new shell commands */
    CMD("dd",          "Data duplicator (dd if=<src> of=<dst> [count=N] [skip=N])", cmd_dd)
    CMD("cksum",       "Calculate CRC32 checksum and byte count",                  cmd_cksum)
    CMD("mkfifo",      "Create a FIFO/named pipe",                                 cmd_mkfifo)
    CMD("logger",      "Log message with timestamp",                               cmd_logger)
    CMD("mesg",        "Control write access to terminal",                         sh_mesg)
    CMD("nproc",       "Print number of processors",                               sh_nproc)
    CMD("pinky",       "Lightweight finger user info",                             sh_pinky)
    CMD("tset",        "Set terminal settings",                                    cmd_tset)
    CMD("reset",       "Reset terminal (clear screen, reset colors)",              cmd_reset)
    /* Phase 10 — new compression/network/system commands */
    CMD("blkid",       "Show block device attributes",                              cmd_blkid)
    CMD("bunzip2",     "Decompress bzip2 file (stub)",                              cmd_bunzip2)
    CMD("bzcat",       "Decompress bzip2 to stdout (stub)",                         cmd_bzcat)
    CMD("chroot",      "Change root directory",                                     cmd_chroot)
    CMD("ftp",         "Simple FTP client (stub)",                                  cmd_ftp)
    CMD("gunzip",      "Decompress gzip file (RLE)",                                cmd_gunzip)
    CMD("gzip",        "Compress file using run-length encoding",                    cmd_gzip)
    CMD("iostat",      "Show I/O statistics",                                       cmd_iostat)
    CMD("ip",          "Show/manipulate routing, devices, addresses",                cmd_ip)
    CMD("lscpu",       "Show CPU architecture info",                                cmd_lscpu)
    CMD("lshw",        "Show hardware configuration",                               cmd_lshw)
    CMD("lzma",        "Compress using LZMA (stub)",                                cmd_lzma)
    CMD("pax",         "Portable archive interchange (stub)",                       cmd_pax)
    CMD("ss",          "Socket statistics",                                          cmd_ss)
    CMD("telnet",      "Telnet client (telnet <host> <port>)",                      cmd_telnet)
    CMD("unlzma",      "Decompress .lzma file (stub)",                              cmd_unlzma)
    CMD("unxz",        "Decompress .xz file (stub)",                                cmd_unxz)
    CMD("unzip",       "List/extract zip files (stub)",                             cmd_unzip)
    CMD("uuencode",    "Encode binary file as text (uuencode)",                     cmd_uuencode)
    CMD("uudecode",    "Decode uuencoded file to binary",                           cmd_uudecode)
    CMD("vmstat",      "Show virtual memory statistics",                            cmd_vmstat)
    CMD("xz",          "Compress using LZMA2 (stub)",                               cmd_xz)
    CMD("zcat",        "Decompress gzip file to stdout (RLE)",                       cmd_zcat)
    /* Phase 11 — new shell commands */
    CMD("404",         "Find files by permission bits",                                 cmd_404)
    CMD("zcmp",        "Compare compressed files (stub)",                               cmd_zcmp)
    CMD("zdiff",       "Diff compressed files (stub)",                                  cmd_zdiff)
    CMD("zegrep",      "Extended grep for compressed files (stub)",                      cmd_zegrep)
    CMD("zfgrep",      "Fixed-string grep for compressed files (stub)",                 cmd_zfgrep)
    CMD("zforce",      "Force .gz extension on files (stub)",                           cmd_zforce)
    CMD("zgrep",       "Grep gzip'd files (stub)",                                     cmd_zgrep)
    CMD("zip",         "Zip files (stub)",                                              cmd_zip)
    CMD("zipcloak",    "Zip encryption (stub)",                                         cmd_zipcloak)
    CMD("zipnote",     "Zip comment editor (stub)",                                     cmd_zipnote)
    CMD("zipsplit",    "Split zip archive (stub)",                                      cmd_zipsplit)
    CMD("less",        "Page through a file",                                           cmd_less)
    CMD("ed",          "Line-oriented text editor (stub)",                              cmd_ed)
    CMD("patch",       "Apply diff patch to file (stub)",                               cmd_patch)
    CMD("pr",          "Paginate/file print (column formatter)",                         cmd_pr_wrapper)
    CMD("look",        "Display lines beginning with given string",                     cmd_look)
    CMD("locale",      "Get locale information",                                        cmd_locale)
    CMD("localedef",   "Define locale (stub)",                                          cmd_localedef)
    CMD("iconv",       "Convert character sets (copy)",                                 cmd_iconv)
    CMD("script",      "Terminal session recording (stub)",                             cmd_script)
    CMD("mcookie",     "Generate random cookie string",                                 cmd_mcookie)
    CMD("shar",        "Create shell archive (encode files as shell script)",           cmd_shar)
    /* Phase 11+ — added utility commands */
    CMD("timeout",     "Run command with time limit (timeout <secs> <cmd> [args...])", cmd_timeout)
    CMD("stdbuf",      "Set I/O buffer size for command (stdbuf -o N <cmd>)",          cmd_stdbuf)
    CMD("b2sum",       "Compute BLAKE2b message digest (b2sum <file>)",                 cmd_b2sum)
    CMD("base32",      "Encode file in base32 (base32 <file>)",                         cmd_base32_wrapper)
    CMD("basenc",      "Encode file with given encoding (basenc --base64 <file>)",      cmd_basenc)
    CMD("chcon",       "Change file SELinux security context (stub)",                    cmd_chcon)
    CMD("runcon",      "Run command with specified security context (stub)",             cmd_runcon)
    CMD("csplit",      "Split file by context lines",                                    cmd_csplit)
    CMD("dircolors",   "Set color ls output (dircolors <file>)",                         cmd_dircolors)
    CMD("install",     "Copy file with attributes (install -m 755 src dst)",            cmd_install)
    CMD("numfmt",      "Convert numbers (numfmt --to=si <value>)",                      cmd_numfmt)
    CMD("ptx",         "Produce permuted index (ptx <file>)",                            cmd_ptx)
    CMD("sha1sum",     "Compute SHA1 message digest (sha1sum <file>)",                   cmd_sha1sum)
    CMD("sha224sum",   "Compute SHA224 message digest (sha224sum <file>)",               cmd_sha224sum)
    CMD("sha384sum",   "Compute SHA384 message digest (sha384sum <file>)",               cmd_sha384sum)
    CMD("sha512sum",   "Compute SHA512 message digest (sha512sum <file>)",               cmd_sha512sum)
    CMD("col",         "Filter reverse line feeds (col <file>)",                        cmd_col)
    CMD("colrm",       "Remove columns from file (colrm start end <file>)",             cmd_colrm)
    CMD("column",      "Columnate file (column -t <file>)",                             cmd_column)
    CMD("crontab",     "Manage per-user crontab files (-l list, -e edit, -r remove)",      cmd_crontab)
    CMD("diff3",       "Compare three files line by line (diff3 f1 f2 f3)",              cmd_diff3)
    CMD("host",        "DNS lookup utility (host <name> [server])",                     cmd_host)
    CMD("init",        "Switch system runlevel (init N)",                               cmd_init)
    CMD("lsattr",      "List extended file attributes (lsattr <file>)",                  cmd_lsattr)
    CMD("lessecho",    "Echo arguments with metacharacter escaping (lessecho)",          cmd_lessecho)
    CMD("lesskey",     "Customize less key bindings (lesskey <file>)",                   cmd_lesskey)
    CMD("ipcrm",       "Remove IPC resources (ipcrm -m|-s|-q <id>)",                    cmd_ipcrm)
    CMD("ipcs",        "Show IPC resources (ipcs -m|-s|-q)",                            cmd_ipcs)
    CMD("isosize",     "Show ISO filesystem size (isosize <device>)",                   cmd_isosize)
    /* Phase 12 — new system utility commands */
    CMD("moc",         "Show memory object cache (slabinfo)",                            cmd_moc)
    CMD("pmap",        "Show process memory map (pmap <pid>)",                           cmd_pmap)
    CMD("pwdx",        "Show process working directory (pwdx <pid>",                    sh_pwdx)
    CMD("flock",       "Manage file locks (flock <fd> <cmd>)",                           cmd_flock)
    CMD("lslocks",     "List kernel file locks (lslocks)",                               cmd_lslocks)
    CMD("nsenter",     "Run command in namespace (stub)",                                cmd_nsenter)
    CMD("unshare",     "Run command in new namespace (stub)",                            cmd_unshare)
    CMD("export",      "Set export attribute for shell variable",                        cmd_export)
    CMD("unset",       "Unset a shell variable",                                        cmd_unset)
    CMD("setsid",      "Run command in new session (setsid <cmd>)",                      cmd_setsid)
    CMD("stdbuf_pipe", "Send to pipe with buffer (stdbuf_pipe <cmd>)",                   cmd_stdbuf_pipe)
    CMD("time_verbose","Detailed timing output (time -v <cmd>)",                         cmd_time_verbose)
    CMD("sysctl",      "Read/write kernel parameters at runtime (sysctl [-a] [-w name=val] [-p file])", sh_sysctl)
    /* Module commands (M21-M22) */
    CMD("insmod",      "Load a kernel module (insmod <path> [params])", sh_insmod)
    CMD("rmmod",       "Unload a kernel module (rmmod <name>)", sh_rmmod)
    CMD("lsmod",       "List loaded kernel modules", sh_lsmod)
    CMD("modinfo",     "Show kernel module information (modinfo [name])", sh_modinfo)
    CMD("modprobe",    "Dependency-aware module loader (modprobe <name> [params])", sh_modprobe)
    /* System logger daemon (U7) */
    CMD("syslogd",     "System logger daemon (syslogd [stop|status])",   cmd_syslogd)
    /* DHCP client daemon (U8) */
    CMD("dhcpcd",      "DHCP client daemon (dhcpcd [stop|status|renew|release])", cmd_dhcpcd)
    /* Internet super-server daemon (U9) */
    CMD("inetd",       "Internet super-server daemon (inetd [stop|status|reload])", cmd_inetd)
    /* Filesystem table (U25) */
    CMD("mount",       "Mount filesystems (mount [-a] [-t fstype] [-o opts] [dev] [mp])", cmd_mount)
    /* Nano text editor (U48) */
    CMD("nano",        "Simple text editor (nano [file])", cmd_nano)
    /* Block device TRIM/discard (Item 330) */
    CMD("blkdiscard",  "Deallocate (TRIM) sectors on a block device (blkdiscard <dev_id> <start_lba> <count>)", cmd_blkdiscard)
    { 0, 0, 0 }
};

#undef CMD
