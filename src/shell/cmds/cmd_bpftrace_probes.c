/* cmd_bpftrace_probes.c — BPF probe management — stub */

#include "shell_cmds.h"
#include "printf.h"

void cmd_bpftrace_probes(const char *args)
{
    (void)args;
    kprintf("bpftrace_probes: not yet implemented\n");
}
