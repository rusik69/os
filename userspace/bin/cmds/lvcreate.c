/* lvcreate.c — LVM logical volume creation stub */
#include "unistd.h"
#include "string.h"
#include "stdio.h"

int main(int argc, char *argv[]) {
    (void)argc;
    (void)argv;
    printf("LVM (Logical Volume Manager) is a kernel feature. "
           "Use LVM tools from the kernel shell.\n");
    return 0;
}
