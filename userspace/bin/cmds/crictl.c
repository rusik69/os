/* crictl.c — container runtime interface */
#include "unistd.h"
#include "stdio.h"
int main(void){
    printf("Container runtime managed by kernel container subsystem (src/container/).\n");
    return 0;
}
